spring:
  application:
    name: conflict-tracker-api

  datasource:
    url: jdbc:h2:mem:conflicttrackerdb
    driver-class-name: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    defer-datasource-initialization: true  # Importante: carga data.sql DESPUÉS de crear tablas

  h2:
    console:
      enabled: true
      path: /h2-console

server:
  port: 8080